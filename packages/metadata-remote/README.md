# @perspectives/metadata-remote

An HTTP-backed implementation of the engine's `MetadataStore` interface, used by the desktop app once it's linked to a workspace on a Perspectives server. Connection profiles and credentials are never sent through it — those stay local.
