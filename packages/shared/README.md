# @perspectives/shared

Cross-package utilities and error types used by the engine, UI, adapters, and metadata stores. Keep this package small — anything specific to one layer belongs in that layer's package.
