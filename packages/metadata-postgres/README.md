# @perspectives/metadata-postgres

A PostgreSQL implementation of the engine's `MetadataStore` interface, used by the self-hostable server and the managed SaaS. Stores workspace-scoped perspectives, memberships, shares, and audit history.
