# @perspectives/server

The self-hostable Perspectives server: exposes the engine and `MetadataStore` over HTTP+tRPC for the desktop app's "linked workspace" mode, and is also the deployable surface for the managed SaaS. Runs against a Postgres metadata store.
